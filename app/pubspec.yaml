name: loose_ends
description: "Privacy-first, on-device commitment tracking app."
publish_to: 'none'
version: 1.0.3+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_rust_bridge: ^2.13.0

dev_dependencies:
  flutter_rust_bridge_codegen: ^2.13.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
