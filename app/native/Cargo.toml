[package]
name = "loose_ends_native"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
loose-ends-core = { path = "../../core", default-features = false }
chrono = "0.4"
serde_json = "1"
jni-sys = { version = "0.4", optional = true }

[features]
jni = ["jni-sys"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[profile.dev]
opt-level = 1
